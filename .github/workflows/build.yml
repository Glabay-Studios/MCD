name: Generating The Midnight docs

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate-docs:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: generated-docs
      - name: Set up Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
      - name: Set up Node
        uses: actions/setup-node@v7
        with:
          node-version: '26'
      - name: Install elm
        run: npm i elm
      - name: Fetch main
        run: |
          git fetch origin main
      - name: Rest score to main
        run: |
          git reset --hard origin/main
      - name: Merge main
        run: |
          git merge origin/main --no-edit
      - name: Build with Gradle
        run: |
          ./compile.sh
      - name: Commit generated file
        run: |
          git add docs/*
          git commit -m "Merge main and add docs/* from Github CI" || echo "No changes to commit"
          git push -f
